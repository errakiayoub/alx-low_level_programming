#include<stdio.h>
/**
 * main-prints all possible combinations of single-digit numbers
 *
 * Return:0 if successful
 */
int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
	
		putchar(c + '0');
	
	if (n != 9)
	{
		putchar(',');
		putchar(' ');
	}
}
putchar('\n');
return (0);
}
